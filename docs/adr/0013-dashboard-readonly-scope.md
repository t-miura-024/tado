# 初期スコープを参照専用（read-only）に限定する

TUIダッシュボードの初期スコープを、ワークフロー定義やセッション状態の作成・変更・削除を一切行わない参照専用に限定する。操作（confirm等）を含めると、状態遷移・TTY制約（ADR-0007）・監査ログ（gate_events）への波及が大きく、初期スコープを超える。

read-onlyとすることで、ダッシュボードは5テーブルへのSELECTのみで完結し、WAL+busy_timeout下でも安全に1秒ポーリングできる。操作が必要になった場合は別計画で合成方式（ADR-0004同様）を検討する。
