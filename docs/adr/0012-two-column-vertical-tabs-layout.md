# レイアウトを2カラム（サイドバー垂直タブ + メインコンテンツ）に固定する

ダッシュボードで「一覧で発見 → 詳細で原因特定」までを一貫して辿るため、画面を2カラムに固定する。左側のサイドバーにセッションを垂直タブとして縦積みし、選択中タブの詳細を右側のメインコンテンツに表示する。

代替として単一リスト＋モーダル詳細は一覧と詳細の往復が煩雑になり、3カラムはターミナル横幅では情報密度が下がるため不採用とした。
